export interface User {
  firstName: string;
  lastName: string;
  birthDate: string;
  address: string;
  postalCode: string;
  city: string;
}

export interface UserDB {
  first_name: string;
  last_name: string;
  birth_date: string;
  address: string;
  postal_code: string;
  city: string;
}
